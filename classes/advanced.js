export default class Advanced {
    constructor (fieldWidth, fieldHeight, minesCount) {
        this.fieldWidth = fieldWidth;
        this.fieldHeight = fieldHeight;
        this.minesCount = minesCount;
    }
}